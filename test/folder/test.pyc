#include ./imp
#include <os>

#define MEGA 16.56

function main(){

    var = MEGA;

    print(10 == 10 && (15 == 10 || 5 > 1));
    print(var);
    print("Hello World!");

    for (i in range(0, 10)){
        print(i);
    }
    
    i = 0;
    while(i < 10){
        i++;
    }

    chungus('c');
}